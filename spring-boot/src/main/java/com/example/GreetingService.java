public interface GreetingService {

    // Use: 
    // - Go to Implementations (Ctrl+F12) to see all classes implementing this interface
    // - Find All Implementations to track actual method bodies in classes like GreetingServiceImpl
    String getGreeting();
}